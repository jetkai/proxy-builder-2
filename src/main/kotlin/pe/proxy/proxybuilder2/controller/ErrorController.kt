package pe.proxy.proxybuilder2.controller

//@ControllerAdvice
class ErrorController {

   /* @ExceptionHandler(value = [(IllegalArgumentException::class)])
    private fun onErrorResponse(exception : IllegalArgumentException) : ResponseEntity<String> =
        ResponseEntity<String>(exception.message?.let { Jsoup.parse(it).text() }, HttpStatus.INTERNAL_SERVER_ERROR)*/

}